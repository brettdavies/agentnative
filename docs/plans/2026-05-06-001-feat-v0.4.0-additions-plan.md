# v0.4.0 — substantive additions to P1, P2, P4, P6 + new P8

Status: IN PROGRESS · 2026-05-06 · MINOR bump (v0.3.1 → v0.4.0; new MUSTs added).

## Progress log

- 2026-05-07 — rebase onto `origin/dev` complete. Used `git rebase --onto origin/dev ab1e8b8 feat/v0.4.0` to drop the 9
  cherry-picks (`1ec1edb..ab1e8b8`, now squash-merged into dev as `f6f04b1` PR #22) and replay the 12 v0.4.0-original
  commits (`109578e..faf1ea1`) on top of `f6f04b1` + `819b1b8`. Conflicts resolved:
- `principles/p1`: kept dev's `SHOULD` capitalization, layered the new `p1-must-secret-non-leaky-path` MUST bullet on
  top.
- `README.md`: kept dev's sentence-break recast (`active`. Published`), updated count to "eight". Folded two more
  `Seven`-references on lines 3 and 16 (added by PR #22 after`86de1a3` was authored) into the same commit because they
  fall within `86de1a3`'s stated intent — "update cross-references for 8 principles". Trevin's "7 Principles" /
  "seven-axis" Acknowledgements references intentionally preserved per the original commit message.
- `principles/AGENTS.md`: same pattern — added `p1` through `p8` correction on line 52 to the same cross-reference
  commit.
- `.impeccable.md`: kept dev's em-dash → semicolon on `framework names` bullet, took 2e4ac83's v0.4.0 → v0.3.1
  attribution correction with the P8-clarification sentence.
- `principles/p2`, `p4`, `p6`, `p8` and the rest auto-merged (frontmatter additions onto prose-cleaned bodies; no
  narrative collisions).
- `VERSION` (0.4.0), `scripts/check-links.mjs`, and `scripts/hooks/pre-push` all dropped from the conflict surface
  because the cherry-picks that touched them are gone — dev's versions land verbatim. Branch is `21..14` vs
  `origin/feat/v0.4.0` (12 v0.4.0 commits + 2 absorbed dev commits). Final diff vs `origin/dev`: 11 files, +455 / -20.

  Next steps gated on Brett's review of the rebased diffs:
- Run pre-push (Vale + LT + pack-README drift + validators) against the new bullets.
- Recast em-dashes in any new prose if `~/.claude/skills/unslop/scripts/score.py` flags density.
- `bun scripts/test-prose-check.mjs` (expect 6/6 OK).
- PR-body authoring in `/tmp/` per `RELEASES.md` § Prose scrubbing; `gh pr create --base dev` after the body passes Vale
+ LT + unslop.

## Summary

v0.3.1 (PR #22) ships the editorial release: prose-refresh on existing P1–P7 bullets, `BRAND.md`, spec-channel
`.impeccable.md`, README `## Acknowledgements`. No new contracts.

v0.4.0 grows the contract. Nine new requirement bullets across P1, P2, P4, P6, plus a new principle **P8 — Discoverable
Through Agent Skill Bundles**. Driven by the source-mining work in `.context/research-2026-05-05/` (synthesis brief,
four parallel source-mining reports, credit-and-deverbatim plan). The substance was agreed in a working session
2026-05-06; this doc captures the canonical wording and execution order.

## Items

### 1. P1 — secrets non-leaky path (MUST conditional)

**Source:** clig.dev lines 640, 837–844. Sprint 2 foundational source-mining flagged this as a direct contradiction with
P1's existing `p1-must-env-var` MUST.

```yaml
- id: p1-must-secret-non-leaky-path
  level: must
  applicability:
    if: CLI accepts secret material (tokens, passwords, keys) as input
  summary: "Sensitive inputs are readable via stdin or a `--*-file` flag; flag-value and env-var inputs MAY exist for convenience but MUST NOT be the only path."
```

**Prose:**

> CLIs that accept secret material — tokens, passwords, private keys — MUST provide at least one input
> path that does not leak the value into process listings (`ps`), shell history, or the parent
> environment. The two leak-resistant paths are stdin and a `--*-file` flag pointing to a credential
> file. Flag-value (`--token <value>`) and environment-variable (`TOOL_TOKEN`) paths MAY exist as
> convenience surfaces but MUST NOT be the only programmatic path. Cloud-CLI env-var conventions
> (`AWS_ACCESS_KEY_ID`, `GH_TOKEN`) are accepted as convenience paths under this rule, not as
> substitutes for it.

**Anti-pattern bullet to add:** A `--password <value>` flag with no stdin or file alternative — every invocation leaks
the secret into `ps` output.

### 2. P2 — schema export, print form (MUST conditional)

**Source:** Vasudevan, "Output formats are a versioned API contract" (InfoQ, 2025-08). Synthesis brief Priority 4d,
pulled forward from v0.5+.

```yaml
- id: p2-must-schema-print
  level: must
  applicability:
    if: CLI emits structured output
  summary: "CLIs that emit structured output expose the output schema via a `schema` subcommand or `--schema` flag — runtime-discoverable, with a documented format identifier."
```

**Prose:**

> CLIs that emit structured output (`--output json|jsonl`) MUST expose the output schema at runtime via
> a `schema` subcommand (or a `--schema` flag on each data-emitting subcommand). The schema MUST
> identify its format — canonical recommendation is JSON Schema 2020-12, the same dialect OpenAPI 3.1
> uses — so an agent reading the schema loads the right validator without parsing prose. A consumer
> asking "what shape am I about to receive?" gets a machine-readable answer in one call.

### 3. P2 — schema export, file form (SHOULD conditional)

```yaml
- id: p2-should-schema-file
  level: should
  applicability:
    if: CLI emits structured output
  summary: "Output schemas are also exported to a stable file path (e.g., `schema/<command>.json`) so CI/static-analysis consumers pin without invoking the tool."
```

**Prose:**

> The schema SHOULD also be exported to a stable file path in the source repo (e.g.,
> `schema/<command>.json`) so consumers can pin against it at install or CI time without invoking the
> tool. The print form is the runtime contract; the file form is the build-time contract.

### 4. P2 — `--json` / `--jsonl` accepted aliases (SHOULD)

**Source:** Cloudflare's "always `--json`, never `--format`" rule (`blog.cloudflare.com_cf-cli-local-explorer.md`);
Trevin v2 §6.

```yaml
- id: p2-should-json-aliases
  level: should
  applicability: universal
  summary: "`--json` and `--jsonl` are accepted as aliases for `--output json` and `--output jsonl` — the short forms work alongside the canonical enum."
```

**Prose:**

> CLIs SHOULD accept `--json` as an alias for `--output json` and `--jsonl` as an alias for
> `--output jsonl`. The `--output` enum remains the canonical surface for the format MUST
> (`p2-must-output-flag`); a Cloudflare-style CLI shipping only the short forms still satisfies the
> canonical MUST through the alias path.

### 5. P4 — enumerate the valid set on closed-set rejections (SHOULD conditional)

**Source:** Trevin v2 §3 "Errors that teach AND enumerate"; HeyGen pattern; synthesis brief.

```yaml
- id: p4-should-enumerate-valid-set
  level: should
  applicability:
    if: CLI rejects input against a closed set
  summary: "When rejecting input against an enum or fixed-allowed-values set, the error message includes the valid set."
```

**Prose:**

> When the failure is "invalid value for X" against a known closed set — an enum field, a documented
> allow-list, a typed parameter — the error SHOULD include the valid set. An agent reading
> `error: invalid visibility` guesses and retries; an agent reading
> `error: --visibility must be one of: public, private, unlisted (got "secret")` self-corrects in one
> round-trip.
>
> ```text
> error: --visibility must be one of: public, private, unlisted (got "secret")
> ```

### 6. P6 — graceful SIGTERM handling (MUST conditional)

**Source:** 12-factor §IX (Disposability); Vasudevan; clig.dev SIGINT ergonomics.

```yaml
- id: p6-must-sigterm
  level: must
  applicability:
    if: CLI has long-running operations
  summary: "Long-running operations handle SIGTERM gracefully — flush or roll back partial writes, release locks, exit non-zero within a bounded window. Next invocation succeeds without manual cleanup."
```

**Prose:**

> Agent harnesses send SIGTERM when their own timeout fires. A CLI that exits abruptly leaving a
> half-written file, a stale `*.tmp` artifact, or a held flock makes the next invocation fail with a
> confusing error the agent cannot diagnose. Long-running operations MUST handle SIGTERM by flushing
> or rolling back partial writes, releasing acquired locks, and exiting non-zero within a bounded
> shutdown window. The next invocation MUST succeed without manual cleanup of the previous run's state.
> This complements the existing SIGPIPE MUST (`p6-must-sigpipe`).

### 7. P6 — standard naming (MAY)

**Source:** clig.dev "Use standard names for flags"; Trevin v2 §6 + Cloudflare; uenyioha (preferable verbs).

```yaml
- id: p6-may-standard-names
  level: may
  applicability:
    if: CLI uses subcommands
  summary: "Subcommand verbs MAY follow community-standard names (`get`/`list`/`create`/`update`/`delete`); flag spellings MAY follow widely-used canonical forms (`--force`, `--yes`, `--limit`, `--quiet`, `--verbose`)."
```

**Prose:**

> Subcommand verbs MAY follow community-standard names — `get` / `list` / `create` / `update` /
> `delete` — and flag spellings MAY follow widely-used canonical forms (`--force` for confirmation
> bypass, `--yes` for prompt bypass, `--limit` for pagination, `--quiet`/`--verbose` for volume
> control). Convergence reduces an agent's per-tool relearning cost: an agent that has seen
> `kubectl get` and `gh repo list` recognizes `tool list` immediately, without re-reading `--help`.

### 8. New P8 — Discoverable Through Agent Skill Bundles

**Source:** Sprint 2 vendor census — five vendors ship `SKILL.md`/`AGENTS.md` bundles (Firecrawl, CLI-Anything, gws,
Crush, larksuite); two ship auto-registration installers (Firecrawl `init --all`, CLI-Anything `npx skills add`). Your
own `agentnative-skill` repo dogfoods the pattern. Synthesis brief Priority 4d alternative; user direction 2026-05-06 to
lift to a new principle.

**File:** `principles/p8-discoverable-skill-bundle.md`. Follow the per-file structure from `principles/AGENTS.md`
(Definition / Why Agents Need It / Requirements / Evidence / Anti-Patterns).

```yaml
---
id: p8
title: Discoverable Through Agent Skill Bundles
last-revised: 2026-05-06
status: active
requirements:
  - id: p8-should-bundle-exists
    level: should
    applicability: universal
    summary: "CLIs ship a top-level agent-discoverable markdown bundle (`AGENTS.md`, `SKILL.md`, or equivalent) with YAML frontmatter naming the tool and capability summary."
  - id: p8-must-bundle-install
    level: must
    applicability:
      if: CLI ships an agent skill bundle
    summary: "When a skill bundle exists, the CLI provides an install path (`tool skill install [<host>]`) that registers the bundle with installed agent runtimes."
  - id: p8-may-install-all
    level: may
    applicability:
      if: CLI ships an agent skill bundle
    summary: "An `--all` mode auto-detects installed runtimes (Claude Code, Cursor, Codex, OpenCode, etc.) and installs across all."
  - id: p8-may-bundle-update
    level: may
    applicability:
      if: CLI ships an agent skill bundle
    summary: "An update/upgrade subcommand (`tool skill update`) pulls the latest bundle version."
---
```

**Definition:** A skill bundle is a structured markdown file (canonical names: `AGENTS.md` or `SKILL.md`) with YAML
frontmatter that names the tool, describes its capabilities, and provides workflow guidance an agent can load into its
runtime. The bundle lives outside the CLI's flag space — agents discover it via filesystem convention, not via `--help`.

**Why agents need it:** `--help` describes what is *possible* (the flag and subcommand surface); a skill bundle
describes what to *do* (workflow knowledge, common compositions, recovery patterns). Workflow knowledge does not fit in
`after_help` examples. Without a bundle, every invocation begins with a `--help` round-trip plus inference; with one,
the agent loads `SKILL.md` once and recognizes the tool's idioms across every subsequent invocation.

**Anti-Patterns:**

- A CLI shipping a skill bundle with no install path — the bundle sits unread until a human manually copies it.
- An install path that requires the agent runtime to be running — `tool skill install` should write to the runtime's
  filesystem cascade (e.g., `~/.claude/skills/`), not require an active session.
- A bundle whose contents drift from the CLI's actual surface — the bundle is part of the CLI's release artifact, not a
  separate doc tree.

The vendor census in `.context/research-2026-05-05/02-source-mining-vendor-community.md` documents the shipped patterns;
agentnative-skill's `bin/check-update` is a reference for the update-check pattern.

### 9. VERSION bump 0.3.1 → 0.4.0

Last commit on the branch. MINOR bump per `principles/AGENTS.md` (new MUSTs added: P1 secrets, P2 schema-print, P6
SIGTERM, P8 install).

### 10. Companion `agentnative-cli` PR (required by coupled-release norm)

The CLI's principle registry (`src/principles/registry.rs`) needs new check IDs for every new requirement above (**11
IDs total: 1 in P1, 3 in P2, 1 in P4, 2 in P6, 4 in P8**). Each gets a check stub. Where check logic can't land
alongside the registry add, mark with a TODO — the registry-drift test fires on missing IDs but doesn't require check
logic to be complete on day one. Coupled-release norm fires hard for this release.

**Tracked as a required follow-up in the CLI repo's local todo tree at
`agentnative-cli/.context/compound-engineering/todos/020-pending-p0-spec-v0-4-0-companion-pr.md`** (local-only by design
— `.context/` is gitignored in every brettdavies repo). The todo lives in the doer's repo, not here, since the work is
entirely on the CLI side: registry edits, `sync-spec.sh`, and the `cargo test` drift gate.

The companion PR is gated, not optional: per `principles/AGENTS.md` § "Coupled-release protocol", the v0.4.0 spec PR
body MUST link the companion PR (or carry the explicit "no check changes needed" justification, which v0.4.0 cannot
because it adds new MUSTs). Order of operations: (a) finalize spec PR; (b) cut companion branch on `agentnative-cli`
once spec PR is review-ready; (c) link both PRs to each other in their bodies; (d) merge spec PR first, companion within
24 hours so registry-drift CI on `agentnative-cli`'s `dev` does not stay red.

## Out of scope (held for v0.5+)

Documented in `.context/research-2026-05-05/03-synthesis-adversarial-brief.md`:

- Trevin v2 §6 cross-CLI vocabulary as SHOULD/MUST (v0.4.0 has only the MAY).
- Trevin v2 §7 three-layer introspection (`agent-context` / `llms.txt`).
- Trevin v2 §8 async-aware execution (`--wait` + job ledger).
- Trevin v2 §9 persistent identity through profiles.
- Trevin v2 §10 two-way I/O (`--deliver` + `feedback`).
- POSIX argument grammar (`--`, `-` operand, option grouping) MUST.
- XDG Base Directory paths MUST.
- Locale-independent JSON output.
- First-byte responsiveness floor (100 ms + progress events).
- Stable error identifier (numeric `code` separate from `kind`).

## Branch and release flow

- Branch: `feat/v0.4.0` from `origin/dev` plus 9 cherry-picked commits from `docs/v0.3.1` (PR #22).
- After PR #22 merges to `dev`, the v0.4.0 branch should rebase onto `dev` to drop the cherry-picked duplicates: `git
  rebase --onto origin/dev <docs/v0.3.1-tip-sha> feat/v0.4.0`.
- v0.4.0 PRs to `dev` per `RELEASES.md`. Cut `release/v0.4.0` from `origin/main` after PR-to-dev merges; cherry-pick; PR
  to `main`. Both PRs require human review and explicit merge — no auto-merge.

## References

- `.context/research-2026-05-05/00-sprint-plan.md`
- `.context/research-2026-05-05/01-trevin-audit.md`
- `.context/research-2026-05-05/02-source-mining-{foundational,standards,agent-cli-wave,vendor-community}.md`
- `.context/research-2026-05-05/03-synthesis-adversarial-brief.md`
- `.context/research-2026-05-05/04-credit-and-deverbatim-plan.md`
- `.context/research-2026-05-05/05-voice-strategy.md`
